/**
 * Shared limits for profile / tree-about image uploads.
 * Client: `storage.service.ts` (browser-image-compression).
 * Server: `profileLikeServerResize.ts` (sharp).
 */
export const PROFILE_UPLOAD_MAX_DIMENSION = 500;
