/** Base style ids — shared by the token registry and variant-id parser. */
export const BASE_STYLE_IDS = [
  'heritage-sky',
  'parchment-classic',
  'gold-royal',
  'olive-mediterranean',
] as const;
